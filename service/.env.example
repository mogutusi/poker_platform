# 基础设施配置(.env)—— 见 docs/config.md / docs/dev.md「两套配置文件」。
#
# 本文件(.env.example)是模板:复制为 service/.env(gitignored)填真值。
# .env 装密钥(生产 DB 连接串 / 未来 JWT 等),绝不提交;改配置项时只更新本 *.example(不含真值)。
# 游戏可调参数走 app/poker.env(另一轨,见 app/poker.env.example)。
#
# DATABASE_URL:不设 / 留空 → 应用用 sqlite+aiosqlite、Alembic 用 sqlite(同一本地 ./poker.db,免 .env 也能跑)。
#   生产设 postgres(psycopg3 同一 URL 兼同步/异步):
#   DATABASE_URL=postgresql+psycopg://user:password@host:5432/poker
DATABASE_URL=
